module.exports = (Model) => {
  const Create = (folder) => { }

  const Delete = (folder) => { }

  const GetTree = () => {}

  const GetParent = (folder) => { }

  const GetChildren = (folder) => {}

  return {
    Name: 'User',
    Create,
    Delete,
    GetTree,
    GetParent,
    GetChildren
  }
}
